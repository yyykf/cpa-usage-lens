# OS / Editor
.DS_Store
*.swp

# Local / private config
.claude/settings.local.json
.env
.env.*
!.env.example

# Per-developer Trellis workspace (journals, session traces)
.trellis/workspace/

# Dependencies / build output (Go backend + React frontend)
node_modules/
dist/
build/
*.log
backend/bin/
*.out

# Removed local preview sandbox (kept ignored to avoid accidental re-add)
local-preview/

# Playwright / E2E 临时产物
.playwright-cli/
/dashboard.yaml
/dashboard-e2e.png
